export default function Settings() {
  return (
    <div className="p-4 bg-white rounded shadow">
      <div className="font-semibold">Settings</div>
      <p className="text-sm text-gray-600">Configure environment variables on the server and client `.env` files. Role bootstrapping via `ADMIN_EMAILS` and `RETAILER_EMAILS`.</p>
    </div>
  );
}
